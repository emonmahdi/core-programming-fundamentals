// Reverse a String (Classic)

function reversString(str){
    let stack = [];
    for(let ch of str){
        stack.push(ch)
    }

    let result = ''
    while(stack.length){
        result += stack.pop()
    }

    return result;
}

// const result =  reversString('hello')
console.log(reversString('hello'))

/* 
ঠিক আছে 👍
আমি **একদম beginner-friendly বাংলায়** ধাপে ধাপে বুঝাচ্ছি — তুমি একদম নতুন হলেও বুঝতে পারবে।

---

## 🔁 এই ফাংশনটা কী করে?

এই কোডটি একটি **string উল্টো করে (reverse)** দেয়

```js
"hello" → "olleh"
```

---

## 🧠 মূল আইডিয়া (Stack ব্যবহার করে)

এখানে আমরা **Stack (LIFO)** ব্যবহার করছি
মানে: **শেষে ঢোকানো অক্ষর আগে বের হবে**

---

# এখন লাইন বাই লাইন ব্যাখ্যা 👇

---

## 1️⃣ Function তৈরি

```js
function reverseString(str) {
```

এখানে একটি function বানানো হয়েছে যার নাম `reverseString`
এটা একটি string নেবে → `str`

---

## 2️⃣ Stack তৈরি

```js
let stack = [];
```

এটা একটা **খালি array**, যেটা আমরা stack হিসেবে ব্যবহার করছি

---

## 3️⃣ String-এর প্রতিটি অক্ষর stack-এ ঢোকানো

```js
for (let ch of str) {
  stack.push(ch);
}
```

ধরো `str = "hello"`

Loop চলবে এভাবে:

| চক্র | ch | stack                 |
| ---- | -- | --------------------- |
| 1    | h  | ["h"]                 |
| 2    | e  | ["h","e"]             |
| 3    | l  | ["h","e","l"]         |
| 4    | l  | ["h","e","l","l"]     |
| 5    | o  | ["h","e","l","l","o"] |

📦 এখন stack = `["h","e","l","l","o"]`

---

## 4️⃣ নতুন খালি string বানানো

```js
let result = "";
```

এখানে আমরা উল্টো string জমা করবো

---

## 5️⃣ Stack থেকে অক্ষর বের করা (pop)

```js
while (stack.length) {
  result += stack.pop();
}
```

এখন stack থেকে **শেষ অক্ষর আগে বের হবে**

ধাপে ধাপে:

| pop() | result  |
| ----- | ------- |
| o     | "o"     |
| l     | "ol"    |
| l     | "oll"   |
| e     | "olle"  |
| h     | "olleh" |

📦 stack খালি হয়ে গেল

---

## 6️⃣ Result return করা

```js
return result;
```

Return হবে:

```js
"olleh"
```

---

## 7️⃣ Output দেখা

```js
console.log(reverseString("hello"));
```

Output:

```
olleh
```

---

# 🔥 কেন Stack ব্যবহার করা হলো?

কারণ Stack কাজ করে:

👉 **Last In → First Out**

যেটা string reverse করার জন্য পারফেক্ট!

---

## 🧑‍💻 সহজ ভাষায় পুরো কাজ:

1. string-এর সব অক্ষর stack-এ ঢুকাও
2. stack থেকে এক এক করে বের করো
3. বের করা অক্ষর নতুন string-এ যোগ করো
4. Done! string reverse

---

## ✅ Interview-এ বলার মতো কথা:

> “I reversed the string using stack and LIFO principle by pushing all characters and popping them to form the reversed string.”

---

## তুমি চাইলে আমি দেখাতে পারি:

* ✔️ Stack ছাড়া reverse
* ✔️ Interview প্রশ্ন
* ✔️ Visualization (diagram)
* ✔️ Practice problem
* ✔️ Real-life use of stack

বললেই হবে 😊



*/